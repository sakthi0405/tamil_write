name: signature_example
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0

  # display signature
  signature: ^3.2.1
  path_provider: ^2.0.8
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true